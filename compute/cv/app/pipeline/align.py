import cv2
import numpy as np
from loguru import logger

def align_image(image):
    # Placeholder: no perspective correction yet
    # This step exists to validate camera skew later
    logger.debug("Alignment skipped (placeholder)")
    return image
