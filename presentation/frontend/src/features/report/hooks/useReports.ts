export function useReports() {
  // Deprecated stub: the Reports feature was refactored.
  return {
    reports: [] as unknown[],
    loading: false,
    error: null as string | null,
    loadReports: async () => {},
  };
}
